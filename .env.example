# .env.example
# 請將此文件複製為 .env 並填寫您的配置信息

# ---------------------------------
# LLM API (OpenAI 相容)
# 用於角色分析、智能分段等。
# ---------------------------------
LLM_API_KEY="your_LLM_API_key_here"
# 如果您使用自建或代理的 OpenAI API，請修改此 URL
LLM_API_BASE_URL="https://api.openai.com/v1"
LLM_DEFAULT_MODEL="gpt-4o-mini"

# ---------------------------------
# POE API
# 用於生成圖片。
# ---------------------------------
# 您的 Poe API 金鑰或 Token
IMAGE_API_KEY="your_poe_token_here"
# Poe API 的請求 URL (請根據實際情況修改)
IMAGE_API_URL="https://api.poe.com/v1"
IMAGE_MODEL="nano-banana"

# ---------------------------------
# TTS (Text-to-Speech) API
# 用於生成語音。
# ---------------------------------
# 如果您使用外部 TTS 服務，請在此處配置 URL。
# 如果留空，系統將使用內置的 edge-tts。
# 例如：https://your-tts-service.com/api/generate
TTS_API_URL=""

# ---------------------------------
# 應用程式設定
# ---------------------------------
# 後端服務器運行的端口
BACKEND_PORT=8000

# 輸出配置
OUTPUT_DIR="outputs"
TEMP_DIR="outputs/temp"
# Note: segment limits and image grouping are controlled by frontend/API
# request payload (`max_segments`, `segments_per_image`), not by env.

# CORS
CORS_ALLOW_ORIGINS="*"
